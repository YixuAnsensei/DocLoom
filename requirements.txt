# 一键安装全部功能（等价于 pip install "docloom[all]"）。
# 只想装核心 / 按需装可选依赖，见 pyproject.toml 的 optional-dependencies。

# 核心（引擎必需）
httpx>=0.27
beautifulsoup4>=4.12

# 可选：PDF/DOCX 解析与抽图（docloom[docs]）
pymupdf>=1.24
python-docx>=1.1

# 可选：Web UI（docloom[web]）
fastapi>=0.110
uvicorn>=0.29
